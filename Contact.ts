export interface Contact {
    name                : string;
    description         : (string|undefined);
    phoneNumbers        : string[];
    emailAddresses      : string[];
}

export default Contact;